# Build artifacts
main.js
*.js.map
dist/
.build/

# Dependencies
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code agent worktrees (internal scratch dir, never tracked)
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Obsidian plugin dev
data.json

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfstate.*.backup
terraform/.terraform.tfstate.lock.info
terraform/*.tfplan
terraform/tfplan
terraform/**/.terraform/
terraform/**/.build/
terraform/modules/lambda/.build/
terraform/environments/*.tfvars
# Auto-loaded by terraform on every plan/apply. We keep environments/*.tfvars
# as the canonical source of truth and copy from there; both are gitignored
# because they contain stage-specific values (admin email, secret ARNs, …).
terraform/terraform.tfvars
terraform/*.auto.tfvars

infrastructure/node_modules/

# Environment secrets
.env
.env.*
*.pem
*.key
credentials.json

# Cache
.vaultguard-cache/
.vaultguard-long-operation-stress/

# Private-only tooling — planning + AI-assistant configs.
# Mirrors scripts/templates/public-gitignore.txt; see also
# scripts/export-validator.mjs::FORBIDDEN_AI_PATHS.
.planning/
.claude/
.codex/
.cursor/
.agents/
.cursorrules
.github/skills/
CLAUDE.md
AGENTS.md
CODEX.md
